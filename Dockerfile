# 1. Базовий образ Node.js (lts)
FROM node:lts

# 2. Робоча директорія всередині контейнера
WORKDIR /app

# 3. Копіюємо лише файли залежностей (для кешування)
COPY package*.json ./

# 4. Встановлюємо залежності
RUN npm install

# 5. Копіюємо весь проєкт
COPY . .

# 6. Відкриваємо порт (наприклад 3000)
EXPOSE 3000

# 7. Команда запуску
CMD ["node", "index.js"]
